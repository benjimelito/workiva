# Design System

Extracted from: https://www.workiva.com/
Generated: 2026-04-08T10:05:10.887Z

## Overview

This design system was automatically extracted by SystemMiner. It captures the visual language of the source site exactly as observed, including any inconsistencies or duplicates.

### Token Statistics
- **Colors**: 48 tokens
- **Typography**: 12 tokens  
- **Spacing**: 7 tokens
- **Effects**: 3 tokens
- **Components**: 34 patterns

## Installation

### 1. CSS Tokens

Import the CSS variables in your `index.css`:

```css
@import './design-system/css/tokens.css';
```

### 2. Tailwind Configuration

Merge the theme extension into your `tailwind.config.ts`:

```typescript
import { systemMinerTheme } from './design-system/tailwind/theme-extend';

export default {
  theme: {
    extend: {
      ...systemMinerTheme,
    },
  },
};
```

### 3. Components

Copy the components folder to your project:

```bash
cp -r design-system/components src/components/extracted/
```

Then import and use:

```tsx
import { Button, Card } from '@/components/extracted';
```

## Token Naming Convention

All tokens use the `--sm-` prefix (SystemMiner) to avoid conflicts:

- `--sm-color-*` - Color tokens
- `--sm-font-*` - Typography tokens
- `--sm-spacing-*` - Spacing tokens
- `--sm-radius-*` - Border radius tokens
- `--sm-shadow-*` - Shadow tokens

## Usage in Tailwind

After merging the theme, use tokens with the `sm-` prefix:

```html
<div class="bg-sm-bg-primary text-sm-text-default p-sm-md rounded-sm-radius-1">
  Content styled with extracted tokens
</div>
```

## Notes

- Tokens are extracted exactly as observed - duplicates and near-duplicates are preserved intentionally
- Component variants are inferred from observed patterns and may need manual adjustment
- This is a descriptive extraction, not a normalization - it mirrors the source, including inconsistencies

---

Generated by [SystemMiner](https://systemminer.lovable.app)
